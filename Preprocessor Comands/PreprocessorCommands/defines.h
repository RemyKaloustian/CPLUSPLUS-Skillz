#define dat_int 46
#define dat_string "wat a string !"

//No need to return, returns automatically the line (here a or b)
#define getmax(a,b) ((a)>(b)?(a):(b))

//the # converts into a string
#define to_str(x) #x